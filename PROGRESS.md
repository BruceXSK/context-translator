# Progress

## Current State

v1 核心已验证通过：悬停翻译 + 划线翻译在 Chrome 中正常工作。CT-001..015、POP-001/006、ARCH-001..006/008/010/011/012 → DONE。剩余：POP-002..005（popup 当前页区：手动上下文输入、token 用量、压缩/清空按钮——content 侧处理已就绪，只缺 popup UI）+ ARCH-007（压缩，待 POP-004 触发验证）。CT-013（line-clamp 容器兼容，Chrome 验证通过 → DONE）：悬停译文块若被 `-webkit-line-clamp` 祖先截断（如 Google 搜索摘要 `.VwiC3b` 两行 clamp），显示时设该祖先 clamp=`none`（仿 immersive-translate 实测机制），隐藏时按记录的原 inline 值恢复（有值则还原、空则移除覆盖让类规则重新生效，避免 `removeProperty` 误删原 inline `2`）。设置页拆分（POP-001/006，Chrome 验证通过 → DONE）：popup 留目标语言 + openOptionsPage 入口，其余配置进独立 open_in_tab 设置页，触发键为 Alt/Shift/Ctrl 分段胶囊单选（存标准 KeyboardEvent.key 名），共用 chrome.storage.local（config 模块），局部 patch 保存互不覆盖；typecheck/build 绿。悬停译文（CT-010/CT-011，Chrome 验证通过 → DONE）：去 Shadow DOM、段内紧贴原文、页面 CSS 字体、略浅透明、无框；内联格式保留用真实标签+`data-ct-id` 占位符（不发 href/class 等）、DOMPurify 消毒、按 id 复用原元素克隆、prompt 禁止丢弃元素（含 the/a/an 合并情况，修 3-链接句首链接丢失）；重试按钮移除、CT-009 收窄到划线悬浮窗。划线悬浮窗不变。typecheck/build 绿。CT-012（悬停加载占位）：首 token 前在译文位显示 `Translating` + braille spinner（10 帧 `⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏`，`window.setInterval` 计时器驱动、非 CSS——因 BlockView light-DOM 无 shadow，全局 keyframes 会污染宿主页；~80ms/帧），首 chunk 到达 `setChunk` 切换为流式文本，`done`/`error` 清计时器；Chrome 验证通过 → DONE。DeepSeek 后端特调文档（design/deepseek.md，DS-001..004 DONE）：DS-001 缓存前缀稳定请求形状、DS-002 多轮不回传 reasoning_content、DS-003 现行模型名示例 v4-flash/v4-pro（deepseek-chat/reasoner 2026/07/24 弃用）、DS-004 规范 base URL https://api.deepseek.com（兼容 /v1）；正文含 Compatibility stance（通用最佳实践 vs DeepSeek 专属，代码对缺失字段容错）、Reference（模型/定价/限速/错误码/thinking/kv_cache/multi-round/prefix-completion 不采用）、Tuning opportunities（友好错误/自动重试/thinking 适配/usage 双重计数，均未实施非 spec）。thinking/effort 可配（CFG-003/BG-005/POP-006 modify/DS-005 → DONE，typecheck/build 绿，Chrome 验证通过）：Settings 加 thinking 开关（默认关，开启时设置页弹通知提示翻译可能变慢）+ effort（low/medium/high/max，默认 low）；sw.ts 请求体始终带 `thinking:{type:enabled|disabled}`（不发则 DeepSeek 默认 enabled，故关闭须显式 disabled）、开时加 `reasoning_effort`；设置页加思考模式 switch + 思考强度分段胶囊单选（4 项），thinking 关时 effort fieldset disabled 灰化；thinking/effort 即时生效（service worker 每请求读）。流式仍只读 delta.content（未做"思考中"反馈）；不设 max_tokens（thinking 下思维链可能耗尽默认预算，记为注意点）。同语言悬停跳过（CT-015/CFG-004，typecheck/build 绿，Chrome 验证通过）：悬停段落已是目标语言时静默不发请求（零 token、不污染 session、不破坏 DeepSeek 缓存前缀）；content 侧纯 Unicode 脚本占比判定（移除 `[lang]` 策略——`closest('[lang]')` 会爬到页面级标签如 GitHub `<html lang="en">`，不代表段落语言，曾导致中文被误翻，且同脚本语言 lang 区分本就不可靠）：目标中文汉字 >20% 即跳过（从 50% 降，兼容中英混排，修 DeepSeek 混排；排除假名/谚文避免 ja/ko 误判），日文看假名、韩文谚文 >30%、西里尔/阿拉伯 >50%、拉丁系 >50% 且无 CJK；仅悬停，划线翻译始终发送；默认开（`skipSameLang`），v1 不暴露设置页。已知局限：同脚本语言对（繁简、en/fr/de）一律按目标脚本跳过，不再区分。Release 自动化（ARCH-012）：`.github/workflows/release.yml` 服务端 build + 打 zip + 发 release，push `v*` tag 触发，版本三者校验；安装方式见 README，release body 留空、`generate_release_notes` 关。首发 `v0.1.0` + 修复版 `v0.1.1` 已发布（v0.1.1 含 baseUrl/model 默认 DeepSeek + 空值回退修复，v0.1.0 不含；CI 12s 绿，`context-translator-0.1.1.zip`；见 https://github.com/BruceXSK/context-translator/releases/tag/v0.1.1）。baseUrl/model 默认 DeepSeek（CFG-002 modify：`https://api.deepseek.com` + `deepseek-v4-flash`，不填或清空保存均经 `withDefaults` 回退默认，仅 API Key 必填）。custom prompt 替代可编辑 system prompt（CFG-002/SES-002/DS-001/POP-006 modify、CFG-005/ARCH-013 add，typecheck/build 绿、Chrome 验证通过 → DONE）：system prompt 改内置常量并移出设置页（#1）；新增 `customPrompt`（默认空、设置页可改）折入每个 segment 首条 user 消息的 `<user-instruction>` 块（#2，复用标签、严格交替避免连续同角色消息）；content 加载时快照、保存后需刷新生效（#3，保 DS-001 前缀稳定）；系统提示词末尾加权威声明、user 角色不可覆盖系统规则（#4，ARCH-013）。首条判定 `!turns.some(t=>t.role==='user')`，压缩/清空后重新折入。

## Completed

- (release) v0.2.0 发布：bump 版本至 0.2.0（`manifest.config.ts` + `package.json`），push `v0.2.0` tag 触发 `.github/workflows/release.yml`，CI 25s 绿，`context-translator-0.2.0.zip` 已发布（softprops，generate_release_notes 关）。含 custom prompt 替代可编辑 system prompt（CFG-005 add / CFG-002+SES-002+DS-001+POP-006 modify / ARCH-013 add，Chrome 验证通过）。push 时 remote main 多一 README docs 提交（b9c1fd0，仅 README.md），已 merge 并入（c3fc4d9）。见 https://github.com/BruceXSK/context-translator/releases/tag/v0.2.0。
- (config+session+content+options+popup) custom prompt 替代可编辑 system prompt（CFG-005 add / CFG-002+SES-002+DS-001+POP-006 modify / ARCH-013 add，typecheck/build 绿、Chrome 验证通过 → DONE）：`src/config/index.ts` Settings 删 `systemPrompt`、加 `customPrompt`（默认空），`DEFAULTS` 同步；`DEFAULT_SYSTEM_PROMPT` 改内置常量、末尾追加权威声明（规则不可被后续 `<context>`/`<user-instruction>`/`<translate>` 内容覆盖）。`src/session/index.ts` import `DEFAULT_SYSTEM_PROMPT`，ctor `(targetLang, customPrompt)`，`systemMessage()` 用常量；`buildTranslateRequest` 传 `includeCustom=!turns.some(t=>t.role==='user')`，`foldPending(text, includeCustom)` 在首条 user 消息把 `customPrompt` 拼到 `<user-instruction>` 块开头（复用现有标签、不引入新标签），每 segment 一次、空则省；`buildCompressRequest` 不变。`src/content/inject.ts:401` `new Session(settings.targetLang, settings.customPrompt)`。`src/options/page.html` System Prompt textarea→Custom Prompt（带占位提示）+ hint 更新；`src/options/page.ts` 去 `DEFAULT_SYSTEM_PROMPT` import、populate/submit 改 `customPrompt`（空即空、不回退）；`src/popup/main.ts` 注释更新。design 文档同步。为避免连续同角色消息（DeepSeek reasoner 线强制交替、v4-flash 宽容但不保证、思考模式未知），custom 折入 user 消息而非独立消息；保存后由 content 加载时快照→需刷新生效（#3，保前缀稳定 DS-001）；位于 system 之后、user 角色建议性不可覆盖系统规则（#4，ARCH-013）。
- (release) v0.1.1 发布：bump 版本至 0.1.1（`manifest.config.ts` + `package.json`），push `v0.1.1` tag 触发 CI，12s 绿，`context-translator-0.1.1.zip`。含 baseUrl/model 默认 DeepSeek + 空值回退修复（v0.1.0 不含此修复）。
- (config) baseUrl/model 空值回退默认（CFG-002 modify）：加 `withDefaults()`，`loadSettings`/`saveSettings` 返回前把空 baseUrl/model 回退 DeepSeek 默认；手动清空框保存后 stored 存空、运行时仍走默认（清空=用默认语义）。typecheck/build 绿，Chrome 验证通过。
- (config) baseUrl/model 默认 DeepSeek（CFG-002 modify）：`src/config/index.ts` DEFAULTS.baseUrl `''`→`'https://api.deepseek.com'`、DEFAULTS.model `''`→`'deepseek-v4-flash'`；apiKey 仍空（必填）。不填即用 DeepSeek，仅 API Key 必填即开箱可用；已填用户 stored 值不受影响（loadSettings 合并时 stored 覆盖 DEFAULTS）。typecheck/build 绿，Chrome 验证通过。
- (release) v0.1.0 首发：push `v0.1.0` tag 触发 `.github/workflows/release.yml`，CI 服务端 build + 版本校验通过 + 打 zip + softprops 发 release，17s 绿；asset `context-translator-0.1.0.zip`（27KB）。Node 20 deprecation warning（actions 被 runner 强制 Node 24，不影响运行）。
- (infra) release 自动化（ARCH-012）：新增 `.github/workflows/release.yml`——push `v*` tag 触发，CI 服务端 `npm ci` → `npm run build` → 校验 tag / `dist/manifest.json` / `package.json` 三者版本一致（不一致 fail）→ dist 打进 `context-translator-<version>/` 顶层目录后 zip → `softprops/action-gh-release@v2` 发 release 挂 zip，`generate_release_notes` false、body 空（安装说明在 README）。版本源 `manifest.config.ts` + `package.json` 手改后打 tag。README 加「从 Release 安装」章节（下载 zip→解压→开发者模式加载已解压；附注 Chrome 禁未上架 .crx 故用加载已解压）。typecheck/build 不涉及 workflow。
- (content) CT-015 移除短文本放行：删掉 `judgeable.length < 4` 提前 return（原为保守避免误杀短文本），改为纯按占比判定——短中文（如「你好」）目标中文也跳过，行为更一致。typecheck/build 绿，Chrome 验证通过。
- (content) CT-015 移除 [lang] 判定 + zh 阈值降至 20%：删掉 `sameLang`/`zhScript`/`scriptSubtag` 一整套，`looksLikeTargetLang` 不再查 `[lang]`（`closest('[lang]')` 会爬到页面级标签如 GitHub `<html lang="en">`，不代表段落语言，曾导致中文内容被误翻；同脚本语言 lang 区分本就不可靠）。改为纯 Unicode 脚本占比判定：目标中文汉字 >20%（从 50% 降，兼容中英混排，修 DeepSeek 混排；排除假名/谚文避免 ja/ko 误判）、日文看假名、韩文谚文 >30%、西里尔/阿拉伯 >50%、拉丁系 >50% 且无 CJK。移除 `el` 参数（调用点同步）。修 GitHub 始终翻译 + DeepSeek 混排翻译。代价：同脚本语言对（繁简、en/fr/de）一律按目标脚本跳过，不再区分。typecheck/build 绿，Chrome 验证通过。
- (content) CT-015 修复 sameLang 误判回归：上次重构后 `sameLang` 要求"双方都无变体"才算同语言，导致页面 `<html lang="zh">`（泛指，最常见写法）+ 目标 `zh-CN` 被判"不同变体"→ 不跳过 → 简中翻简中。放宽规则：同 primary subtag 即同语言（跳过），仅不同 4 字母 script 变体（zh-Hans/zh-Hant、sr-Latn/sr-Cyrl）才判可能需要翻译；region 变体（en-US/en-GB）视为同语言。中文特殊：Hans/Hant 既可 script 子标签也可 region（CN/SG→简，TW/HK/MO→繁）表达，加 `zhScript` 归一化再比；加 `scriptSubtag` 提取通用脚本子标签。验证：zh/zh-CN/zh-Hans→zh-CN 跳过，zh-TW/zh-Hant→zh-CN 翻译，en→en、zh→en 等不变。typecheck/build 绿，待 Chrome 验证。
- (content) CT-015 修复 lang 信号误跳过：`looksLikeTargetLang` 原先 `[lang]` 属性独立短路（`closest('[lang]')` 爬到祖先命中 `sameLang` 即 `return true`），导致目标 en、悬停中文、页面 `<html lang="en">` 时被误判同语言跳过、不翻译。重构为脚本占比先判、`lang` 仅在脚本通过后做变体细化（无 lang 或同目标→跳过；不同变体→不跳过）。修用户 bug：目标 en 悬停中文现正常翻译；保留 `lang` 对繁简（zh-CN/zh-TW）与拉丁系语言（en/fr）区分。typecheck/build 绿，待 Chrome 验证。
- (content+config) 同语言悬停跳过（CT-015/CFG-004）：`src/config/index.ts` Settings 加 `skipSameLang:boolean`（默认 true）+ DEFAULTS 同步；`src/content/inject.ts` 加 `looksLikeTargetLang(text, el, targetLang)`（`[lang]` 属性精确/同 primary 无变体匹配 + Unicode 脚本占比：zh 看 Han>50% 且 kana/hangul=0、ja 看 kana、ko 看 hangul>30%、ru/uk/be/bg 看 Cyrillic>50%、ar 看 Arabic>50%、其余拉丁系看 Latin>50% 且无 CJK；未知 targetLang 永不跳过；可判定字符<4 放行）+ `sameLang` + `countScripts`；`doToggle` 命中 hit 后、`toggleHover` 前若 `settings.skipSameLang && looksLikeTargetLang(stripTags(hit.skeleton), hit.el, settings.targetLang)` 则静默 return。仅悬停；划线翻译始终发送。无 UI 反馈。v1 不暴露设置页。typecheck/build 绿，Chrome 验证通过。
- (options) thinking 默认改关 + 开启通知（CFG-003/POP-006 modify）：`src/config/index.ts` DEFAULTS.thinking true→false；`src/options/page.html` thinking checkbox 去 checked、effort fieldset 初始 disabled、加 `#toast` 元素 + `.toast` CSS（顶部 fixed、accent 左边框、8s 自动隐 + close 按钮）；`src/options/page.ts` thinking change listener 在 checked 时 `showThinkingNotice()` 显示 toast（populate 程序设值不触发 change，故仅用户手动开启才弹），close 按钮 + 8s timeout 清。toast 文案润色：模型先推理再翻译，等待首字期间长时间停留在「Translating…」属正常现象，思考额外耗 token 按输出计费，推荐保持关闭。typecheck/build 绿，Chrome 验证通过。
- (config+background+options) thinking/effort 可配（CFG-003/BG-005/POP-006 modify/DS-005）：`src/config/index.ts` Settings 加 `thinking:boolean`（默认 true）+ `effort:string`（默认 'low'），DEFAULTS 同步；`src/background/sw.ts` 请求体始终带 `thinking:{type: settings.thinking?'enabled':'disabled'}`，开时加 `reasoning_effort:settings.effort`（不发 thinking 则 DeepSeek 默认 enabled，故关闭须显式 disabled）；`src/options/page.html` 加思考模式 switch（纯 CSS `.switch`+`.track` 滑块，checked 切 accent）+ 思考强度 `.segmented` 单选 4 项 Low/Medium/High/Max（存小写）+ `fieldset.effort[disabled]` 灰化 CSS；`src/options/page.ts` populate 读 thinking/effort + `syncEffortDisabled()` 按 thinking checkbox 切 effort fieldset.disabled、change listener 联动、submit patch 加 `thinking:fd.get('thinking')==='on'` + effort 校验。thinking/effort 即时生效（sw 每请求 loadSettings 读，非 content 快照）。流式仍只读 delta.content（未做思考中反馈）；不设 max_tokens（注意点）。typecheck/build 绿，Chrome 验证通过。
- (docs) design/deepseek.md + DESIGN.md：新增 DeepSeek 后端特调设计文档（DS 前缀注册）——DS-001 缓存前缀稳定请求形状（system 前置页面内恒定 + committed 顺序追加 + buffered 上下文末尾 + 不截断，深化 ARCH-006 对齐 KV-cache 前缀单元模型）、DS-002 多轮不回传 reasoning_content（思维链不进前缀、不破坏缓存命中，符合 thinking_mode 无 tool calls 指导）、DS-003 现行模型名示例 v4-flash/v4-pro（规避 2026/07/24 弃用的 deepseek-chat/reasoner）、DS-004 规范 base URL https://api.deepseek.com（兼容 /v1）；正文含 Compatibility stance（通用最佳实践 vs DeepSeek 专属，normalizeUsage 仅在字段存在时捕获、不带 thinking 走默认，故其他后端不受影响）、Reference（模型 1M/384K + 弃用、定价 flash 命中1/50 pro 1/120、限速并发 2500/500 + 10min 超时、错误码 400/401/402/422/429/500/503、thinking 顶层字段 + reasoning 流式 + temp 被忽略 + 思维链按输出价、kv_cache 完整前缀单元匹配 + hit/miss 字段、multi-round 手动拼完整历史、prefix-completion 有意不采用、token 近似）、Tuning opportunities（友好错误/自动重试/thinking 适配/usage 双重计数，均未实施非 spec）。DESIGN.md Modules Index 加 deepseek \| DS 行。同批前序：A 提交 12312dc 刷新 README/config 注释/design/config.md/options placeholder 的模型名与 base URL 示例至 v4-flash + 无 /v1。
- (content) src/content/inject.ts：CT-014 已翻译区域不重复创建译文块——`findParagraph` block 命中分支先 `node.querySelector('context-translator-block')`，若命中返回其 `parentElement`（= 首次 append 的 el）的 ParaHit，否则原逻辑。译文块 `parentElement` 恒等于首次 el，`paraState` 必命中首次 state，toggle 走缓存不 re-request。修复 CT-013 引入场景下的 `findParagraph` 漂移回归（clamp toggle + 译文块插入位移 + `-webkit-box` 不被 block 判定命中 → 向上爬过真实段落到更大容器、`paraState` key 不同 → 重复创建第二个块）。immersive 线索印证：immersive 翻译后 clamp 持续 none、不 toggle，落点稳定故不漂移。typecheck/build 绿，Chrome 验证通过 → DONE。
- (rename) 全局重命名 Chrome Translator → Context Translator——`manifest.config.ts` name、`popup`/`options` 的 `title`+`h1`、`package.json` name（`chrome-translator`→`context-translator`）、自定义元素名 `chrome-translator-block`/`-panel`→`context-translator-block`/`-panel`（`inject.ts` `createElement`×2 + `closest` + `querySelectorAll`）、`DESIGN.md` 结构图根目录引用、`package-lock.json` 同步。保留：物理根目录名 `chrome-translator` 与 GitHub repo `BruceXSK/chrome-translator.git`（确认保留）、`manifest` description 的 'Chrome extension' 与 `DESIGN.md` purpose 中文「Chrome 翻译插件」（Chrome 指浏览器平台，非产品名）。
- (content) src/content/inject.ts：CT-013 line-clamp 容器兼容——`findClamp` 从命中段落向上找首个 `-webkit-line-clamp` 非 none 祖先（如 Google 搜索 `.VwiC3b` 两行 clamp）；`ParaState` 加 `clampEl`+`clampOrigInline`（首次记原 inline 值）；`setClamp(open)` 开时 `setProperty('none')`、关时按原值恢复（inline 有值则 `setProperty(原值)`、空则 `removeProperty` 让类规则重新生效，避免 `removeProperty` 误删原 inline `2`）；`toggleHover` shown→hidden 调 `setClamp(false)`、hidden→shown 调 `setClamp(true)`，`fetchHover` 开头 `setClamp(true)`。机制对齐 immersive-translate（实测其把同一祖先 clamp 设为 none）。原文显示时随 clamp 放开而展开、隐藏时回两行截断。typecheck/build 绿，Chrome 验证通过 → DONE。
- (popup/options) src/popup + src/options + manifest.config.ts：拆分设置入口（POP-001 收窄 + POP-006 新增）——`manifest.config.ts` 加 `options_ui: { page: 'src/options/page.html', open_in_tab: true }`；新增 `src/options/page.html`+`page.ts` 承载 baseUrl/apiKey/model/triggerKey/systemPrompt，复用 config 的 `loadSettings`/`saveSettings`（局部 patch 保存，保留 popup 的 targetLang）；popup 收窄为 targetLang 选框 + 「设置…」按钮（`chrome.runtime.openOptionsPage()`），`main.ts` 改为只读写 targetLang。入口 basename 用 `page.html`/`page.ts` 避与 popup `index.html`/`main.ts` 冲突（呼应 ARCH troubleshooting）。tsconfig `include:["src"]` 覆盖 options；typecheck/build 绿，产物 manifest 含 options_ui，Chrome 验证通过 → DONE。触发键为 Alt/Shift/Ctrl 分段胶囊单选（隐藏原生圆点，`:has(input:checked)` 切 accent 实色，存标准 key 名 `Alt`/`Shift`/`Control`）。
- (content) src/content/inject.ts：CT-012 悬停加载占位——`BlockView.start()` 立即写 `Translating` + braille spinner（静态 `SPINNER='⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏'`，`window.setInterval` 驱动 `waitFrame=(waitFrame+1)%10`，~80ms/帧），首 chunk（`setChunk`）/`done`/`error` 各调 `stopWaiting()` 清计时器。用计时器而非 CSS `@keyframes`：BlockView 是 light-DOM 无 shadow（CT-010），全局 `<style>` 会与宿主页 keyframes 冲突/泄漏；`textEl` 复用（`opacity:.85`、继承页面字体），等待态与译文同位同风格。typecheck/build 绿，Chrome 验证通过 → DONE。
- (content) src/content/inject.ts：悬停 + solo-tap 触发（默认 Alt，可编辑元素内抑制）、段落抽取、译文块插入 + 三态切换 + 按段缓存、划线悬浮面板（单个/可拖拽/可关闭）、Shadow DOM + 构造样式表、流式渲染、原位错误 + 重试、session 持有、contextMenu/popup/compress 消息处理。CT-001..009 已实现，typecheck/build 绿，Chrome 验证通过 → DONE。
- (content) src/content/inject.ts：CT-010/CT-011 源文匹配 + 内联格式保留（方案 B'，真实标签占位符）——`extractSkeleton`/`toSkeleton` 遍历段落把每个内联元素替换为**保留真实标签 + `data-ct-id`** 的占位符（嵌套递归，不发 href/class/data-*/style 等原属性），原元素 `cloneNode(false)`（标签+全部属性）按 id 存本地。`reconstruct`/`reconstructNode` 把 LLM 返回经 DOMPurify（`FORBID_TAGS` 剥 script/style/iframe 等危险标签，`on*` 默认剥、`ALLOW_DATA_ATTR`）后，按 `data-ct-id` 取原元素克隆、`sanitizeClone`（剥 `on*`/`javascript:`/`vbscript:`/`file:`/`data:` href、危险 src）、子内容递归换成译文；id 缺失/未知 → 该处回退纯文本。`BlockView` 持 `originals`，`done` 用 `reconstruct`（空回退 stripTags）。`findParagraph` 返回 `{el,skeleton,originals}`，悬停落自身译文时 `closest` 跳过。system prompt 改写：译文留在元素内、整体移动元素、不让标点/连词进出元素；code/kbd/samp/var 原样。块级错误内联、无重试按钮。PanelView（划线悬浮窗）不变。typecheck/build 绿，Chrome 验证通过 → DONE。
- (popup) src/popup/index.html + main.ts：设置表单（base URL/API key/model/目标语言/触发键/system prompt），读写 chrome.storage.local。POP-001 已实现，typecheck/build 绿，Chrome 验证通过 → DONE。
- (session) src/session/index.ts：Session 类（turns + pending 缓冲、<context>/<user-instruction>/<translate> 折入、commit、压缩为摘要、用量累计）；SES-001..005 → DONE。运行时往返待 content 接线验证。
- (background) src/background/index.ts：Port 流式 chat completions（SSE 解析 + stream_options.include_usage）、API key 托管、右键菜单翻译/理解、DeepSeek 感知用量回传；BG-001..004 → DONE。运行时 LLM 往返待 content 接线验证。
- (config) src/config/index.ts：Settings schema、DEFAULTS、DEFAULT_SYSTEM_PROMPT、COMPRESS_PROMPT、langLabel、loadSettings/saveSettings（chrome.storage.local，不同步）；typecheck + build 绿。CFG-001/002 → DONE。端到端设置往返待 popup 接线（POP-001）时确认。
- (infra) scaffolded TS + Vite + @crxjs/vite-plugin MV3 骨架；typecheck + build 绿，dist/manifest.json 合规。ARCH-009 → DONE。
- (docs) 创建 DESIGN.md + design/{background,content,session,popup,config}.md + PROGRESS.md，写入 ARCH / BG / CT / SES / POP / CFG 全部 spec。

## Unresolved Problems

- 无。

## Spec Changes

- SES-002 (2026-06-25): `[背景]/[翻译]` markers → `<context>`/`<user-instruction>`/`<translate>` XML tags; pending split into context (from 理解) and instruction (from popup) kinds.
- CT-010 (2026-06-25): added — source-matched inline translation; the hover translation drops its container box/label/background and renders directly after the source text within the paragraph, inheriting the source paragraph's typography via the page's CSS, rendered slightly lighter than the source via a transparency effect.
- CT-011 (2026-06-25): added — inline-format-preserving translation via real-tag placeholders + reuse: inline elements keep their real tag + data-ct-id (no other attributes — href/class/data-*/style not sent); returned HTML is sanitized (dangerous tags/handlers stripped) and each tagged element swapped back for a clone of its original (event handlers / dangerous URIs stripped) with recursively reconstructed children; plain-text fallback on loss/unsafe. (Revised from opaque <x> placeholders to real tags after <x> caused the LLM to shuffle/drop placeholders on word-order changes.)
- CT-006 (2026-06-25): modified — shadow isolation scoped to the selection floating panel; the hover translation now renders in the page DOM (no shadow) to inherit the page's styles.
- ARCH-010 (2026-06-25): modified — LLM output changed from pure translation to translated HTML preserving the inline-element markup (real tags + data-ct-id).
- CT-009 (2026-06-25): modified — retry control scoped to the selection floating panel; the embedded hover block no longer shows a retry button and retries by re-pressing the trigger key.
- CT-012 (2026-06-25): added — hover loading placeholder; the embedded hover block shows a "Translating" placeholder followed by an animated braille spinner (10 frames ⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏, ~80ms) from trigger press until the first token, then replaced by the streamed text and cleared on done/error. Timer-driven (not CSS) because the block is light-DOM with no shadow (CT-010). (Revised from dot-count cycling to a braille spinner for a livelier, less monotonous feel.)
- POP-001 (2026-06-25): modified — popup scope narrowed from the full settings form to the target-language selector plus an entry to open the settings page; base URL/API key/model/trigger key/system prompt moved to the options page (POP-006).
- POP-006 (2026-06-25): added — standalone options page (open_in_tab) hosting base URL/API key/model/trigger key/system prompt. Trigger key is a single-select of Alt/Shift/Ctrl (default Alt), stored as the standard KeyboardEvent.key name (Alt/Shift/Control) so the content-script key matcher (e.key) hits; reads/writes the same chrome.storage.local via the config module (shared schema with the popup); changes take effect browser-wide — immediate for endpoint/key/model (the stateless service worker reads them per request), trigger key and system prompt apply to pages loaded after the change.
- CT-013 (2026-06-25): added — line-clamp container compat; showing a hover translation whose block would be clipped by a -webkit-line-clamp ancestor sets that ancestor's clamp to none (confirmed against immersive-translate, which sets the same ancestor's clamp to none); hiding restores the original inline value (re-set it, or remove the override so a class rule re-applies).
- CT-014 (2026-06-25): added — no duplicate translation block; re-hovering an already-translated region toggles the existing block instead of creating a second one. findParagraph re-targets the existing context-translator-block's paragraph (parentElement) when a candidate block already contains a prior translation. Fixes a regression exposed by CT-013 (clamp toggle + layout shift + -webkit-box not matched as block caused findParagraph to climb past the real paragraph and create a duplicate).
- DS-001 (2026-06-26): added — cache-prefix-stable request shape; requests keep DeepSeek's context-cache prefix unit stable (system leads and is page-constant, committed turns append in order, buffered context tails the user message, history never auto-truncated). Deepens ARCH-006 for the DeepSeek KV-cache prefix-unit model.
- DS-002 (2026-06-26): added — no reasoning_content roundtrip; multi-round history carries only the assistant's final content, never reasoning_content, so thinking-mode chains neither enter the prefix nor break cache hits (matches DeepSeek thinking-mode guidance for tool-free turns).
- DS-003 (2026-06-26): added — current model-name examples; examples and placeholders use deepseek-v4-flash (or v4-pro), not deepseek-chat/deepseek-reasoner (deprecated 2026/07/24).
- DS-004 (2026-06-26): added — canonical base-url example; example base URL is https://api.deepseek.com (no /v1), URL joining still tolerates a user-supplied /v1.
- CFG-003 (2026-06-26): added — thinking + effort settings; Settings carry a thinking toggle (boolean, default on) and an effort level (low/medium/high/max, default low), persisted in chrome.storage.local.
- BG-005 (2026-06-26): added — thinking + reasoning_effort in request body; the body always carries `thinking:{type:"enabled"|"disabled"}` (DeepSeek defaults to enabled when omitted, so disabling must be explicit), plus `reasoning_effort:<effort>` when on.
- POP-006 (2026-06-26): modified — options page now also hosts a thinking toggle (switch style, default on) and an effort single-select (segmented capsule, Low/Medium/High/Max, default Low, stored lowercase); effort is disabled and greyed when thinking is off; thinking/effort take effect immediately (service worker reads them per request).
- DS-005 (2026-06-26): added — configurable thinking/effort; DeepSeek thinking top-level field + reasoning_effort are user-configurable, effort full range for portability (DeepSeek honors High/Max, low/medium map to high); thinking field always sent (DeepSeek defaults enabled when omitted, disabling requires explicit {type:"disabled"}).
- CFG-003 (2026-06-26): modified — thinking toggle default changed from on to off.
- POP-006 (2026-06-26): modified — thinking toggle default off; turning it on shows a notice toast warning that translations may stay on "Translating…" longer while the model thinks.
- CT-015 (2026-06-26): added — skip same-language hover; hovering a paragraph already in the target language does not send a request (no token spend, no session pollution, no cache-prefix disruption). Client-side detection in doToggle before toggleHover via (1) a matching [lang] attribute (exact, or same primary subtag with no region variant on either side) and (2) a Unicode-script ratio test on the paragraph's plain text (Han for zh-*, kana for ja, hangul for ko, Cyrillic for ru/uk/be/bg, Arabic for ar, Latin for the rest; unknown target never skips); <4 judgeable chars let through; silent, no UI feedback; hover only, selection always sends; gated by CFG-004 default on, not exposed in settings UI in v1.
- CFG-004 (2026-06-26): added — skipSameLang boolean setting (default true) gating CT-015; not exposed in the options UI in v1.
- CT-015 (2026-06-26): modified — the `[lang]` attribute is no longer an independent skip shortcut (it could climb via `closest('[lang]')` to a mismatched ancestor like `<html lang="en">` over Chinese content and wrongly skip it when the target was English); it is now consulted only after the Unicode-script test passes, purely to refine the variant (absent or matching the target → skip; different variant → translate).
- CT-015 (2026-06-26): modified (2) — `sameLang` relaxed: same primary subtag is now the same language (skip), with only a differing 4-letter script subtag (zh-Hans vs zh-Hant, sr-Latn vs sr-Cyrl) suppressing the skip; region variants treated as the same language. Chinese Hans/Hant is resolved from both script and region subtags (CN/SG → Hans, TW/HK/MO → Hant) before comparing. Fixes a regression where generic `<html lang="zh">` against target `zh-CN` was wrongly treated as a different variant and translated Simplified→Simplified.
- CT-015 (2026-06-26): modified (3) — removed the `[lang]` attribute check entirely (closest('[lang]') climbs to page-level tags like GitHub's <html lang="en"> that don't describe the paragraph, forcing Chinese content to be translated; same-script language distinction via lang was unreliable anyway). Detection is now a pure Unicode-script ratio; the zh threshold dropped from 50% to 20% so CJK-heavy mixed Chinese/English text still skips. Trade-off: same-script language pairs (Traditional vs Simplified, en/fr/de) are no longer distinguished — all skip by target script. Removed sameLang/zhScript/scriptSubtag and the el parameter.
- CT-015 (2026-06-26): modified (4) — removed the <4-judgeable-chars short-text pass-through; detection is now pure script ratio regardless of length (short target-language text like 「你好」 now skips too).
- ARCH-012 (2026-06-26): added — release automation; a GitHub Actions workflow builds and publishes on GitHub servers, triggered by a v* tag, with a version triple check (tag/manifest/package) and a versioned-dir zip attached to a softprops release (generate_release_notes off, no body; install instructions live in README).
- CFG-002 (2026-06-26): modified — default base URL and model are now `https://api.deepseek.com` and `deepseek-v4-flash` (DeepSeek as the primary backend, overridable); API key remains empty by default and must be filled.
- CFG-002 (2026-06-26): modified (2) — empty base URL / model (including after the user clears and saves) fall back to the DeepSeek defaults via withDefaults in loadSettings/saveSettings, so a cleared field still resolves to the default rather than breaking requests.
- CFG-002 (2026-06-29): modified (3) — "built-in default system prompt" -> "built-in system prompt constant (NOT user-overridable, NOT stored in settings — baked into the code; see ARCH-013)"; plus a default custom prompt (empty, see CFG-005) added.
- CFG-005 (2026-06-29): added — customPrompt setting; user-supplementary guidance (default empty), folded into the first user message of a session-segment as part of the existing <user-instruction> block (no new tag), snapshotted on page load (refresh to take effect, DS-001), once per segment (re-added after compress/clear), omitted when empty; supplements but cannot override the system rules (ARCH-013).
- SES-002 (2026-06-29): modified — the first user message of a session-segment (no committed user turns; also first after compress/clear) now leads its <user-instruction> block with the persistent custom prompt (CFG-005), once per segment, omitted when empty, reusing the existing <user-instruction> tag (no new tag).
- DS-001 (2026-06-29): modified — the stable prefix description now includes the custom prompt leading the first user message's <user-instruction> block (constant per page, CFG-005), with buffered context/instruction within the user message.
- POP-006 (2026-06-29): modified (3) — options page hosts a custom prompt (default empty, CFG-005) in place of the system prompt; "trigger key and system prompt apply to pages loaded after the change" -> "trigger key and custom prompt" (the system prompt is now a code constant, no snapshot needed).
- ARCH-013 (2026-06-29): added — system-prompt authority; the system prompt is a non-editable code constant with highest authority, user-editable content (custom prompt + folded context/instruction blocks) is user-role advisory and cannot override the system rules; the system prompt ends with an explicit authority declaration, defending against user prompt-injection breaking the markup-preserving translation (ARCH-010).